import { AccountsScreen } from "@/components/equity/screens";

export const metadata = {
  title: "Accounts & Cards — Equity Mobile",
  description: "View all your Equity accounts, balances and linked cards.",
};

export default function Page() {
  return <AccountsScreen />;
}
