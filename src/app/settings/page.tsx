import { SettingsScreen } from "@/components/equity/screens";

export const metadata = {
  title: "Settings & more — Equity Mobile",
  description: "Manage your Equity profile, notifications and support options.",
};

export default function Page() {
  return <SettingsScreen />;
}
