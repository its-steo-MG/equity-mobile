import { LoginScreen } from "@/components/equity/screens";

export const metadata = {
  title: "Sign in — Equity Mobile",
  description: "Sign in to load your real Equity balances and transactions.",
};

export default function Page() {
  return <LoginScreen />;
}
